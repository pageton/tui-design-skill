---
description: Design and build polished terminal user interfaces (TUI)
---

Use the `tui-design` skill from the installed skills pack at
`~/.codex/skills/tui-design/`. Load `SKILL.md` from that directory, then
load the relevant files from its `references/` and `frameworks/`
subdirectories before generating code or design advice. If the skill pack
is not installed there, tell the user to run the install steps from the
repo's README first.

Target: $ARGUMENTS
If no target is provided, ask the user what TUI they want to build,
redesign, or review.
